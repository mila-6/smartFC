from rag_search import rag_search

# Deprecated compatibility shim.
# RAG.py is retained for backward compatibility and simply re-exports
# the implementation in rag_search.py (the canonical module).
# Delete this file in a future cleanup once all imports use rag_search.
